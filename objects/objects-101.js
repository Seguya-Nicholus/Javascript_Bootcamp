let myPerson = {
    name:'Seguya Nicholus',
    age:32,
    location:'Zziru, Kawuku Entebbe'
} 

console.log(`${myPerson.name} is ${myPerson.age} and lives in ${myPerson.location}`)

myPerson.age = myPerson.age + 1

console.log(`${myPerson.name} is ${myPerson.age} and lives in ${myPerson.location}`)